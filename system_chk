check system $HOST
        alert hardware@dicecorp.com on { resource } with reminder on 300 cycles
        alert telephony@dicecorp.com on { resource } with reminder on 300 cycles
        if memory usage > 75% then alert
